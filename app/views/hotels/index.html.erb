<!DOCTYPE html>
<html lang="en" ng-app='app'>
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>Hotel Urbano Development</title>

		<%= stylesheet_link_tag "bootstrap" %>
		<%= stylesheet_link_tag "hotel_urbano" %>
		<%= stylesheet_link_tag "jquery-ui" %>
		<%= stylesheet_link_tag "theme" %>

		<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
		<script src="https://ajax.googleapis.com/ajax/libs/jqueryui/1.11.3/jquery-ui.min.js"></script>

		<%= javascript_include_tag "angular" %>
		<%= javascript_include_tag "directives" %>
		<%= javascript_include_tag "home.js" %>

		<%= csrf_meta_tags %>
	</head>
	<body ng-controller='homeCtrl'>

		<header>
			<nav class="navbar navbar-inverse navbar-fixed-top">
			  <div class="container">
			  	<div class="navbar-header">
			      <a class="navbar-brand" href="#"> Hoteis</a>
			    </div>
			    <form class="navbar-form navbar-left">
					<div class="form-group">
						<input type="text" class="form-control" ng-model='searchTerm' placeholder="cidade ou hotel">
						<input type="text" class="form-control" datepicker ng-model='checkIn' placeholder="entrada">
						<input type="text" class="form-control" datepicker ng-model='checkOut'  placeholder="saída">
					</div>
					<input type="button" class="btn btn-default" value="pesquisar \o/" ng-click='findOnPeriod()' />
				</form>	
			  </div>
			</nav>
		</header>

		<div class="container">
			<div class='repeat' ng-repeat="hotel in hotels">
				<div class="hotel-container">
				    <div class="hotel-image"><img src="{{hotel.image_path}}" alt=""></div>
				    <div class="text">
				        <p>{{hotel.name}} - {{hotel.description}}</p>
				    </div>
				</div>
			</div>
		</div>
		
		
	</body>
</html>